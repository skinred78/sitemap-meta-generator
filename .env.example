# Vertex AI Configuration
# No API key needed! Uses Application Default Credentials (ADC)
#
# For local development, authenticate with:
#   gcloud auth application-default login
#
# Optional: Override project ID (defaults to sitemap-meta-generator)
# GOOGLE_CLOUD_PROJECT=your-project-id
#
# Optional: Override Vertex AI location (defaults to us-central1)
# Note: Use us-central1 where Gemini models are available
# VERTEX_AI_LOCATION=us-central1
